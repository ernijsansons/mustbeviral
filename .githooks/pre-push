#!/bin/bash
# Simple, non-blocking pre-push hook

echo "🚀 Pre-push validation (non-blocking)"

# Basic environment check
if [ -f "package.json" ] && [ -d "node_modules" ]; then
    echo "✅ Root environment OK"
else
    echo "⚠️ Root environment incomplete, allowing push"
fi

if [ -f "mustbeviral/package.json" ] && [ -d "mustbeviral/node_modules" ]; then
    echo "✅ Mustbeviral environment OK"
else
    echo "⚠️ Mustbeviral environment incomplete, allowing push"
fi

echo "✅ Pre-push validation complete (allowing push)"
exit 0

