# Recovery verification — 2026-09-08

Checkout `C:/dev/MustBeViral`, branch `codex/viralgraph-cleanroom`, HEAD `8578e93`.
Node 24.18.0 and pnpm 11.12.0 through Corepack.

## Completed checks

- Initial `pnpm agent:preflight`: passed; one worktree, 37 registered documents,
  15 transition receipts, WP-P3-009 p3i-003 current.
- Execution-layer checker: passed, ending “Execution layer complete and internally
  consistent.” This is structural validation, not proof that its behavioral
  requirements or approval sequence work.
- `pnpm agent:verify`: exit 0. It ran design validation, governance checks and the
  full `pnpm verify` chain: formatting, governance tests, cleanroom/security, task
  graph, lint, type checking, tests, integration tests and builds.
- Integration suites executed during the run: web 21, contracts 3, Core 2, all
  passing. Build graph reported 18 of 18 successful, all 18 reused from Turbo
  cache. Cache reuse is not represented as 18 newly executed builds.
- `git diff --check`: passed before the final evidence/handoff update.
- Independent read-only review of the recovery artifacts found no new material
  issue; references resolved and successor content matched the validated source.

The first full verification predates the final evidence additions and handoff
projection. The first post-handoff verification stopped at formatting:
`agent:handoff` serialized `ACTIVE_WORK_PACKET.yaml` and `PROJECT_STATE.yaml`
with formatting that Prettier rejected. Independent `pnpm governance:check`
passed. Both files were reformatted;
this was a document-formatting failure, not an application or database failure.
The final `pnpm agent:verify` retry completed successfully at approximately
03:28Z on September 8, exit 0, after the replacement baseline, corrected procedure,
handoff and generated projections were in place. Governance ran 131 tests;
integration suites again passed 21 web, 3 contracts and 2 Core tests. The final
build graph reported 18 of 18 successful, all cached. The command's final
authority/fingerprint confirmation also passed.

This result paragraph was appended afterward; no implementation or authority
changed after that final run. Formatting, diff scope and whitespace checks cover
the final evidence edit before the recovery commit.

## Separate inherited CI failure

At the recovered HEAD, Governance run 33921461621 passed; Quality run 33921461611
failed. Its `quality` job passed, while `database-pgtap` failed in suites 00029,
00032 and 00034. Logs show respectively UUID/text operator mismatch, permission
denied on `oauth_access_tokens`, and `FORBIDDEN`. No Supabase files were changed in
this evidence packet; the planned repair belongs to a subsequent authorized slice.

Local verification does not include `pnpm supabase:test` and does not turn that
remote failure green. No release, migration, provider run, payment, or traffic
change was performed. Original observation acceptance remains unproven.
